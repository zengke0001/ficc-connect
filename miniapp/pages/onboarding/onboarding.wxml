<!-- pages/onboarding/onboarding.wxml -->
<view class="container onboarding">

  <!-- Welcome Step -->
  <block wx:if="{{step === 0}}">
    <view class="hero gradient-blue">
      <view class="hero-emoji">🏆</view>
      <view class="hero-title">FICC Connect</view>
      <view class="hero-subtitle">一起打卡，一起成长</view>
    </view>

    <view class="features">
      <view class="feature-item">
        <text class="feature-icon">🔥</text>
        <view class="feature-text">
          <view class="feature-title">每日打卡</view>
          <view class="feature-desc">坚持打卡，积累连击，赢得荣耀</view>
        </view>
      </view>
      <view class="feature-item">
        <text class="feature-icon">📸</text>
        <view class="feature-text">
          <view class="feature-title">分享照片</view>
          <view class="feature-desc">记录精彩瞬间，收获点赞</view>
        </view>
      </view>
      <view class="feature-item">
        <text class="feature-icon">🏅</text>
        <view class="feature-text">
          <view class="feature-title">排行榜</view>
          <view class="feature-desc">与同事竞技，角逐活动冠军</view>
        </view>
      </view>
    </view>

    <view class="login-area safe-bottom">
      <button class="btn btn-primary btn-block btn-wechat"
              loading="{{loading}}"
              bindtap="handleLogin">
        <text>微信一键登录</text>
      </button>
      <view class="tip">登录即同意相关服务协议</view>
    </view>
  </block>

  <!-- Team Select Step -->
  <block wx:if="{{step === 1}}">
    <view class="team-header">
      <view class="team-title">选择您的部门</view>
      <view class="team-subtitle">方便参与团队挑战赛</view>
    </view>

    <view class="team-grid">
      <view wx:for="{{teams}}" wx:key="id"
            class="team-item {{selectedTeamId === item.id ? 'selected' : ''}}"
            style="border-color: {{selectedTeamId === item.id ? item.color : 'transparent'}};"
            bindtap="selectTeam"
            data-id="{{item.id}}">
        <view class="team-dot" style="background: {{item.color}};"></view>
        <view class="team-name">{{item.name}}</view>
        <view class="team-check" wx:if="{{selectedTeamId === item.id}}">✓</view>
      </view>
    </view>

    <view class="team-action safe-bottom">
      <button class="btn btn-primary btn-block"
              disabled="{{!selectedTeamId}}"
              bindtap="confirmTeam">
        确认，开始挑战！🚀
      </button>
    </view>
  </block>

</view>
