/* pages/checkin/checkin.wxss */
.checkin-page { background: var(--bg-secondary); min-height: 100vh; }

.act-header { position: relative; height: 300rpx; }
.act-cover-img { width: 100%; height: 100%; }
.act-overlay {
  position: absolute; bottom: 0; left: 0; right: 0;
  padding: 30rpx 32rpx;
  background: linear-gradient(transparent, rgba(0,0,0,0.6));
}
.act-name { font-size: 34rpx; font-weight: 700; color: white; }
.act-date { font-size: 24rpx; color: rgba(255,255,255,0.8); margin-top: 6rpx; }

.section-card { margin: 24rpx 24rpx 0; }
.card-label { font-size: 26rpx; font-weight: 600; color: #64748B; margin-bottom: 20rpx; }

.photo-picker {
  height: 280rpx;
  border: 3rpx dashed #CBD5E1;
  border-radius: 20rpx;
  display: flex; align-items: center; justify-content: center;
  background: #F8FAFC;
}
.picker-inner { display: flex; flex-direction: column; align-items: center; gap: 12rpx; }
.picker-icon { font-size: 64rpx; }
.picker-text { font-size: 28rpx; color: #64748B; font-weight: 500; }
.picker-hint { font-size: 22rpx; color: #94A3B8; }

.photo-preview { position: relative; height: 360rpx; border-radius: 20rpx; overflow: hidden; }
.preview-img { width: 100%; height: 100%; }
.remove-photo {
  position: absolute; top: 16rpx; right: 16rpx;
  width: 56rpx; height: 56rpx;
  background: rgba(0,0,0,0.5); color: white;
  border-radius: 50%; font-size: 24rpx;
  display: flex; align-items: center; justify-content: center;
}

.comment-input {
  width: 100%;
  min-height: 120rpx;
  font-size: 28rpx;
  color: #1E293B;
  line-height: 1.6;
}
.input-placeholder { color: #CBD5E1; }
.char-count { text-align: right; font-size: 22rpx; color: #94A3B8; margin-top: 12rpx; }

.submit-area { padding: 40rpx 24rpx 32rpx; }

/* Success overlay */
.success-overlay {
  position: fixed; top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.6);
  display: flex; align-items: center; justify-content: center;
  z-index: 100;
  backdrop-filter: blur(4px);
}
.success-card {
  background: white;
  border-radius: 40rpx;
  padding: 60rpx 48rpx;
  margin: 0 48rpx;
  text-align: center;
}
.success-emoji { font-size: 100rpx; margin-bottom: 20rpx; }
.success-title { font-size: 44rpx; font-weight: 800; color: #1E293B; margin-bottom: 40rpx; }

.success-stats { display: flex; justify-content: center; align-items: center; gap: 40rpx; margin-bottom: 32rpx; }
.stat-item { text-align: center; }
.stat-value { font-size: 48rpx; font-weight: 800; }
.stat-label { font-size: 24rpx; color: #94A3B8; margin-top: 8rpx; }
.stat-divider { width: 2rpx; height: 80rpx; background: #E2E8F0; }
.gradient-orange-text { color: #F59E0B; }
.gradient-fire-text { color: #EF4444; }

.streak-bonus {
  background: #FFFBEB;
  color: #F59E0B;
  border-radius: 100rpx;
  padding: 14rpx 28rpx;
  font-size: 26rpx;
  font-weight: 600;
  margin-bottom: 24rpx;
}

.new-achievements {
  background: #F5F3FF;
  border-radius: 20rpx;
  padding: 24rpx;
  margin-bottom: 32rpx;
  text-align: left;
}
.achievements-title { font-size: 26rpx; font-weight: 700; color: #7C3AED; margin-bottom: 16rpx; }
.achievement-item {
  display: flex; justify-content: space-between;
  font-size: 26rpx; color: #1E293B; padding: 8rpx 0;
}
.ach-points { color: #7C3AED; font-weight: 600; }

.success-btn { margin-top: 8rpx; }
